import { Text, View } from 'react-native';

export default function ConfigNotFound() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Configuration not found</Text>
    </View>
  );
}
