import { Text, View } from 'react-native';

export default function CardiologyCalculatorNotFound() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Cardiology calculator not found</Text>
    </View>
  );
}
